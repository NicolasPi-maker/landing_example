---
const { url, ariaLabel, outlined = false, label, customClass } = Astro.props;
---

<a href={url} aria-label={ariaLabel} class=`${outlined ? 'border text-white border-white' : 'bg-white text-primary'} py-2 px-4 rounded-full text-center h-fit ${customClass}`>{label}</a>
